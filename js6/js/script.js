//stampa il cubo dei primi n numeri dove n è un numero indicato dall'uente


var numbers = 0;
var numeri = prompt("chiedi un numero");

console.log(numbers);
var numbers = 0;


for (i = 0; i < 6; i++) {
    var num_utente = numbers[i];
    console.log(numbers.lenght - 1);

    var msgUscita = document.getElementById('lista').innerHTML;

    document.getElementById('lista').innerHTML = msgUscita + "<li>" + " " + num_utente + "</li>";


}